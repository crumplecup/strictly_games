// AUTO-GENERATED by `elicitation generate verus` — DO NOT EDIT
//
// Standalone Verus proof for CrapsMachine (V13 assume_specification pattern).
// Regenerate: elicitation generate verus --crate-path <root>
//
// No imports from the VSM crate — self-contained for vargo builds.
// Architecture:
//   V11/V12 leaf + composition proof grounded by V13 assume_specification.
//   Each stub is trusted axiomatically (requires/ensures mirrors the
//   formal_method contract); Kani and Creusot independently verify the
//   real transition bodies in elicit_server.

use verus_builtin_macros::verus;
use vstd::prelude::*;

// ─── External transition stubs ─────────────────────────────────────────────────
// These functions represent the real transitions in elicit_server.
// Verus does not verify their bodies; assume_specification below injects
// the trusted contracts.  Kani/Creusot independently verify the real bodies.

/// Stub for `craps_start_betting` — body is opaque to Verus.
#[verifier::external]
pub fn craps_start_betting_stub(state: CrapsState) -> CrapsState {
    todo!()
}

/// Stub for `craps_place_bets` — body is opaque to Verus.
#[verifier::external]
pub fn craps_place_bets_stub(state: CrapsState) -> CrapsState {
    todo!()
}

/// Stub for `craps_comeout_roll` — body is opaque to Verus.
#[verifier::external]
pub fn craps_comeout_roll_stub(state: CrapsState) -> CrapsState {
    todo!()
}

/// Stub for `craps_point_roll` — body is opaque to Verus.
#[verifier::external]
pub fn craps_point_roll_stub(state: CrapsState) -> CrapsState {
    todo!()
}

/// Stub for `craps_next_round` — body is opaque to Verus.
#[verifier::external]
pub fn craps_next_round_stub(state: CrapsState) -> CrapsState {
    todo!()
}

verus! {

/// Abstract mirror of `CrapsState` (invariant-relevant variants only).
#[allow(unused_imports)]
use vstd::prelude::SpecOrd;

#[derive(Debug, Clone, PartialEq, Eq)]
pub enum CrapsState {
    Setup { num_seats: usize },
    Betting { shooter_idx: usize, bankrolls: Vec<u64> },
    ComeOut { shooter_idx: usize, bankrolls: Vec<u64> },
    PointPhase { shooter_idx: usize, bankrolls: Vec<u64> },
    Resolved { shooter_idx: usize, bankrolls: Vec<u64> },
}

/// Invariant for `CrapsMachine`.
pub open spec fn craps_consistent(state: &CrapsState) -> bool { match *state { CrapsState::Setup { num_seats, .. } => num_seats@ > 0, CrapsState::Betting { shooter_idx, bankrolls, .. } => shooter_idx@ < bankrolls@.len(), CrapsState::ComeOut { shooter_idx, bankrolls, .. } => shooter_idx@ < bankrolls@.len(), CrapsState::PointPhase { shooter_idx, bankrolls, .. } => shooter_idx@ < bankrolls@.len(), CrapsState::Resolved { shooter_idx, bankrolls, .. } => shooter_idx@ < bankrolls@.len(), } }

// ─── Postcondition predicates ──────────────────────────────────────────────────

/// Any non-special post-state — the `_ => true` arm of the invariant applies.
pub open spec fn craps_post_trivial(post: CrapsState) -> bool { !(post matches CrapsState::Setup { .. }) && !(post matches CrapsState::Betting { .. }) && !(post matches CrapsState::ComeOut { .. }) && !(post matches CrapsState::PointPhase { .. }) && !(post matches CrapsState::Resolved { .. }) }

/// `Setup` post with non-violating field (invariant vacuously satisfied).
pub open spec fn craps_post_setup_false(post: CrapsState) -> bool {
    match post {
        CrapsState::Setup { num_seats, .. } => num_seats@ > 0,
        _ => false,
    }
}

/// `Betting` post with non-violating field (invariant vacuously satisfied).
pub open spec fn craps_post_betting_false(post: CrapsState) -> bool {
    match post {
        CrapsState::Betting { shooter_idx, bankrolls, .. } => shooter_idx@ < bankrolls@.len(),
        _ => false,
    }
}

/// `ComeOut` post with non-violating field (invariant vacuously satisfied).
pub open spec fn craps_post_come_out_false(post: CrapsState) -> bool {
    match post {
        CrapsState::ComeOut { shooter_idx, bankrolls, .. } => shooter_idx@ < bankrolls@.len(),
        _ => false,
    }
}

/// `PointPhase` post with non-violating field (invariant vacuously satisfied).
pub open spec fn craps_post_point_phase_false(post: CrapsState) -> bool {
    match post {
        CrapsState::PointPhase { shooter_idx, bankrolls, .. } => shooter_idx@ < bankrolls@.len(),
        _ => false,
    }
}

/// `Resolved` post with non-violating field (invariant vacuously satisfied).
pub open spec fn craps_post_resolved_false(post: CrapsState) -> bool {
    match post {
        CrapsState::Resolved { shooter_idx, bankrolls, .. } => shooter_idx@ < bankrolls@.len(),
        _ => false,
    }
}

// ─── Leaf lemmas ────────────────────────────────────────────────────────────────

/// Trivial post satisfies the invariant (`_ => true` arm applies).
pub proof fn craps_leaf_trivial(post: CrapsState)
    requires craps_post_trivial(post),
    ensures  craps_consistent(&post),
{}

/// `Setup` with non-violating field — invariant is vacuously satisfied.
pub proof fn craps_leaf_setup_false(post: CrapsState)
    requires craps_post_setup_false(post),
    ensures  craps_consistent(&post),
{}

/// `Betting` with non-violating field — invariant is vacuously satisfied.
pub proof fn craps_leaf_betting_false(post: CrapsState)
    requires craps_post_betting_false(post),
    ensures  craps_consistent(&post),
{}

/// `ComeOut` with non-violating field — invariant is vacuously satisfied.
pub proof fn craps_leaf_come_out_false(post: CrapsState)
    requires craps_post_come_out_false(post),
    ensures  craps_consistent(&post),
{}

/// `PointPhase` with non-violating field — invariant is vacuously satisfied.
pub proof fn craps_leaf_point_phase_false(post: CrapsState)
    requires craps_post_point_phase_false(post),
    ensures  craps_consistent(&post),
{}

/// `Resolved` with non-violating field — invariant is vacuously satisfied.
pub proof fn craps_leaf_resolved_false(post: CrapsState)
    requires craps_post_resolved_false(post),
    ensures  craps_consistent(&post),
{}

// ─── Assume specifications (V13 trust anchors) ──────────────────────────────────

// Each assume_specification axiomatizes the contract for its stub.
// The postcondition predicate is the same one used by the leaf lemma above,
// ensuring Verus proof and assume_specification are tightly aligned.

/// Contract for `craps_start_betting_stub` — mirrors the formal_method contract that
/// Kani and Creusot independently verify on the real `craps_start_betting` body.
pub assume_specification[craps_start_betting_stub](state: CrapsState) -> (r: CrapsState)
    requires craps_consistent(&state),
    ensures  craps_post_setup_false(r);

/// Contract for `craps_place_bets_stub` — mirrors the formal_method contract that
/// Kani and Creusot independently verify on the real `craps_place_bets` body.
pub assume_specification[craps_place_bets_stub](state: CrapsState) -> (r: CrapsState)
    requires craps_consistent(&state),
    ensures  craps_post_betting_false(r);

/// Contract for `craps_comeout_roll_stub` — mirrors the formal_method contract that
/// Kani and Creusot independently verify on the real `craps_comeout_roll` body.
pub assume_specification[craps_comeout_roll_stub](state: CrapsState) -> (r: CrapsState)
    requires craps_consistent(&state),
    ensures  craps_post_come_out_false(r);

/// Contract for `craps_point_roll_stub` — mirrors the formal_method contract that
/// Kani and Creusot independently verify on the real `craps_point_roll` body.
pub assume_specification[craps_point_roll_stub](state: CrapsState) -> (r: CrapsState)
    requires craps_consistent(&state),
    ensures  craps_post_point_phase_false(r);

/// Contract for `craps_next_round_stub` — mirrors the formal_method contract that
/// Kani and Creusot independently verify on the real `craps_next_round` body.
pub assume_specification[craps_next_round_stub](state: CrapsState) -> (r: CrapsState)
    requires craps_consistent(&state),
    ensures  craps_post_betting_false(r);

// ─── Verified exec callers ───────────────────────────────────────────────────────

// These exec fns close the proof loop: calling any stub on a consistent
// pre-state yields a consistent post-state, by the assume_specification.

/// Proof that `craps_start_betting` preserves the invariant (via assume_specification).
pub fn craps_start_betting_verified(state: CrapsState) -> (r: CrapsState)
    requires craps_consistent(&state),
    ensures  craps_consistent(&r),
{
    craps_start_betting_stub(state)
}

/// Proof that `craps_place_bets` preserves the invariant (via assume_specification).
pub fn craps_place_bets_verified(state: CrapsState) -> (r: CrapsState)
    requires craps_consistent(&state),
    ensures  craps_consistent(&r),
{
    craps_place_bets_stub(state)
}

/// Proof that `craps_comeout_roll` preserves the invariant (via assume_specification).
pub fn craps_comeout_roll_verified(state: CrapsState) -> (r: CrapsState)
    requires craps_consistent(&state),
    ensures  craps_consistent(&r),
{
    craps_comeout_roll_stub(state)
}

/// Proof that `craps_point_roll` preserves the invariant (via assume_specification).
pub fn craps_point_roll_verified(state: CrapsState) -> (r: CrapsState)
    requires craps_consistent(&state),
    ensures  craps_consistent(&r),
{
    craps_point_roll_stub(state)
}

/// Proof that `craps_next_round` preserves the invariant (via assume_specification).
pub fn craps_next_round_verified(state: CrapsState) -> (r: CrapsState)
    requires craps_consistent(&state),
    ensures  craps_consistent(&r),
{
    craps_next_round_stub(state)
}

// ─── Transition tags + composition ──────────────────────────────────────────────

/// One tag per `CrapsMachine` transition — dispatch without re-verifying bodies.
#[derive(Debug, Clone, Copy, PartialEq, Eq)]
pub enum CrapsMachineTrans {
    CrapsStartBetting,
    CrapsPlaceBets,
    CrapsComeoutRoll,
    CrapsPointRoll,
    CrapsNextRound,
}

/// Maps each transition tag to its postcondition predicate.
pub open spec fn craps_post(post: CrapsState, tag: CrapsMachineTrans) -> bool {
    match tag {
        CrapsMachineTrans::CrapsStartBetting => craps_post_setup_false(post),
        CrapsMachineTrans::CrapsPlaceBets => craps_post_betting_false(post),
        CrapsMachineTrans::CrapsComeoutRoll => craps_post_come_out_false(post),
        CrapsMachineTrans::CrapsPointRoll => craps_post_point_phase_false(post),
        CrapsMachineTrans::CrapsNextRound => craps_post_betting_false(post),
    }
}

/// Composition: any post-state produced by a tagged transition satisfies the invariant.
pub proof fn craps_composition(post: CrapsState, tag: CrapsMachineTrans)
    requires
        craps_post(post, tag),
    ensures craps_consistent(&post),
{
    match tag {
        CrapsMachineTrans::CrapsStartBetting => craps_leaf_setup_false(post),
        CrapsMachineTrans::CrapsPlaceBets => craps_leaf_betting_false(post),
        CrapsMachineTrans::CrapsComeoutRoll => craps_leaf_come_out_false(post),
        CrapsMachineTrans::CrapsPointRoll => craps_leaf_point_phase_false(post),
        CrapsMachineTrans::CrapsNextRound => craps_leaf_betting_false(post),
    }
}

} // verus!
